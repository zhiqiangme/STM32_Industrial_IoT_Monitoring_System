# 任务清单已固化（共 18 项，覆盖阶段 0–5）

---

## 待你最终拍板的两件事

1. **架构选择题**：确认走**路线 A（保留 STM32 聚合，G780s 只轮询 STM32 一台 slave）**？  
   → 我已默认按 A 编排所有阶段；如果想走 B（G780s 直接轮询变送器），需要重新写阶段 1 的点表和重新规划 STM32 BSP 改动。
   用户回复：可以。

2. **二期 TLS 是否本期一起做**？  
   G780s 原生支持 TLS 1.2，本期一起做只多 0.5 天，但能避免 1883 公网密码泄漏风险。建议**本期一起做**。
   用户回复：可以一起做。

确认完这两点，我从阶段 0 开始按 todo 顺序逐步实施。