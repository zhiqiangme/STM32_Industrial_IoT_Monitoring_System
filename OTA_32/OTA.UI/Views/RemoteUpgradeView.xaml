<UserControl x:Class="OTA.UI.Views.RemoteUpgradeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="20,0,20,20">
        <Border Margin="0,24,0,0" Style="{StaticResource PanelBorderStyle}">
            <StackPanel>
                <TextBlock Style="{StaticResource H1TextStyle}" Text="{Binding Title}" />
                <TextBlock Style="{StaticResource HintTextStyle}"
                           Margin="0,0,0,20"
                           Text="{Binding Description}" />

                <Border Background="#F9FAFB" BorderBrush="#E5E7EB" BorderThickness="1" CornerRadius="8" Padding="18">
                    <StackPanel>
                        <TextBlock Style="{StaticResource H2TextStyle}" Text="{Binding StatusTitle}" Margin="0,0,0,10" />
                        <TextBlock Style="{StaticResource HintTextStyle}" Text="{Binding StatusText}" />
                    </StackPanel>
                </Border>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
